[project]
name = "bars"
version = "0.1.0"
description = "Always-on Robinhood equity minute-bar collector"
requires-python = ">=3.10"
dependencies = [
    "httpx2>=2.5,<3",
    "mcp>=2,<3",
    "pydantic>=2.12,<3",
]

[tool.uv]
package = false
